#!/bin/sh

# if xbanish exists and is not yet running, run it in background
hasX && \
	[ -x "$(command -v xbanish)" ] && \
	[ "$(ps -a | grep xbanish)" = "" ] && \
	{ log "xbanish started"; xbanish & }

