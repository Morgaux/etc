#!/bin/sh

~/bin/log "Launching start up applications..."

# if xbanish exists and is not yet running, run it in background
hasX && \
	[ -x "$(command -v xbanish)" ] && \
	[ "$(ps -a | grep xbanish)" = "" ] && \
	{ ~/bin/log "xbanish started"; xbanish & }

